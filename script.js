const gameBoard = (() => {
  let gameboard = [];
  //  const players = [];

  const newGame = () => {
    gameboard = [
      [0, 0, 0],
      [0, 0, 0],
      [0, 0, 0],
    ];
  };

//   const addPlayers = (newPlayer) => {
//     players.push(newPlayer);
//   };
})();

// const player = (name) => ({ name });
